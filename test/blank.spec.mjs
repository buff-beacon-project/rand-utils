/* global describe, it */

import chai from 'chai'

chai.expect()

const expect = chai.expect

describe('Change ME', () => {
  describe('Some description', () => {
    it('should do stuff', () => {
      expect(1).to.be.equal(1)
    })
  })
})
